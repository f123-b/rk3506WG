# RK3506 Linux 环境监测与边缘网关项目面试题库

> 项目：my_test / Environment Monitor
> 平台：RK3506G Linux
> 版本：3.1.4
> 代码责任：根据本人确认，项目全部工作由本人独立完成。
> 用途：项目面试准备、AI 面试助手和项目复盘

## 使用说明

本文档中的问题全部围绕当前项目源码。答案采用第一人称，方便面试时直接表达。

回答时要区分“源码已经实现”“源码级检查完成”“目标板或压力测试待补”和“当前明确没有实现”。没有线上日志、测试报告、设备记录或性能报告时，不补充生产事故和具体指标。

---

# 一、项目整体

## 1. 请介绍一下这个项目

**参考回答：**

我独立完成的是一个运行在 RK3506G Linux 平台上的环境监测与边缘数据网关项目，使用 C11 和 CMake 构建。

系统集成了 MQTT、Modbus RTU、SocketCAN、SQLite、HTTP、NTP、OTA 和 LVGL 本地界面。整体采用单进程多线程架构，后台线程负责网络和总线数据采集，Data Bus 用于模块间解耦，主线程负责 LVGL UI 更新。

项目中我重点处理了线程同步、数据记录死锁、后台线程访问 LVGL、OTA 原子更新和资源清理等问题。

## 2. 项目的整体架构是什么？

**参考回答：**

系统可以分为四层：硬件抽象层、基础设施层、业务服务层和应用层。

硬件抽象层包括 DRM、evdev、RS485 和 SocketCAN；基础设施层包括日志、Watchdog、NTP、SQLite 和配置解析；业务服务层包括 MQTT、Modbus、CAN、Data Bus、数据记录器和 OTA；应用层包括主线程、LVGL 页面和 HTTP API。

正常运行时是一个主进程，内部包含多个线程。数据通过 Data Bus 或受保护的共享缓冲传递给数据库、Web 和 UI。

## 3. 为什么采用单进程多线程？

**参考回答：**

多个模块需要共享传感器数据、运行状态、Data Bus 和 UI 数据缓冲，使用线程可以减少进程间通信开销，也适合资源受限的嵌入式设备。

线程的代价是共享数据必须严格同步，所以我补充了 mutex、atomic、条件变量、线程停止和 join 机制。

当前项目没有使用 systemd、Docker 或日常业务多进程 IPC。OTA 中的 fork/exec 只是更新切换流程。

## 4. 你个人负责了哪些内容？

**参考回答：**

这个项目全部由我独立完成。我负责整体架构、模块划分、MQTT、Modbus、CAN、HTTP、NTP、SQLite、OTA、LVGL 页面、Data Bus、线程同步、CMake 构建入口、问题修复和项目文档。

我可以明确说自己独立负责项目，但不会额外补充没有证据的公司、团队规模、项目周期、出货量或客户信息。

## 5. 项目中最难解决的技术问题是什么？

**参考回答：**

最难的不是单个协议接入，而是多个后台 I/O 线程和 UI、数据总线、数据库之间的并发协作。

典型问题有：数据记录器持锁时递归申请同一把锁，Data Bus 在锁内执行回调，以及 CAN 接收线程可能直接访问 LVGL。我分别通过拆分锁内刷盘函数、锁外执行回调、后台数据投递和主线程更新 UI 解决。

## 6. 项目已经实现了哪些功能？哪些没有实现？

**参考回答：**

已经实现并有调用链的功能包括 MQTT、Modbus RTU、SocketCAN、SQLite 数据记录、LVGL 页面、有限 HTTP API、NTP、OTA 和日志轮转。

当前没有宣传完整 Web 历史查询、Modbus 总线扫描、虚假随机寄存器数据显示、CAN UI 过滤器、HTTP 认证、HTTPS、MQTT TLS、OTA 数字签名安全和性能指标。

## 7. 项目如何启动、运行和退出？

**参考回答：**

主线程先初始化日志、数据库、Data Bus、显示、触摸和业务服务，然后启动 MQTT、Modbus、CAN、NTP、HTTP、Watchdog 和数据记录相关线程，最后进入 LVGL 主循环。

退出时由信号处理函数设置退出标志，主线程在正常上下文中执行 app_cleanup，停止服务、等待线程退出、关闭设备并释放资源。

停止顺序是：

~~~text
设置运行状态为停止
    -> 唤醒或等待阻塞线程退出
    -> join 线程
    -> 关闭 fd 和设备
    -> 释放对象和锁
~~~

## 8. 项目有没有使用 systemd、Docker 或多进程 IPC？

**参考回答：**

当前项目没有 systemd unit、Dockerfile、消息队列、共享内存、管道或 Unix Domain Socket 的实现证据。

它是单进程多线程嵌入式应用。OTA 中的 fork/exec 属于更新交接，不是正常业务的多进程架构。

## 9. 如果重新设计，你会保留当前架构吗？

**参考回答：**

如果项目规模和设备资源不变，我会保留单进程多线程和 Data Bus 的总体方向，因为它适合当前共享状态和本地 UI。

我会进一步增加高频 CAN 的环形队列、HTTP 和 OTA 的安全能力，以及单元测试、并发测试和目标板压力测试。如果未来要求强故障隔离，再考虑把 HTTP 或 OTA 拆成独立进程。

## 10. 项目中的数据流是怎样的？

**参考回答：**

以 MQTT 温湿度为例：

~~~text
MQTT 网络循环
    -> MQTT 消息回调
    -> cJSON 解析
    -> on_sensor_data()
    -> 更新传感器状态
    -> Web 状态更新
    -> Data Recorder
    -> 主线程定时器更新 LVGL
~~~

Modbus 和 CAN 数据也会经过业务回调或 Data Bus，后台线程不直接操作 LVGL。

---

# 二、并发和线程安全

## 11. Data Recorder 的死锁是怎么产生的？

**参考回答：**

原来的问题是 data_recorder_record 持有 rec_mutex 时调用公开的 data_recorder_flush，而公开 flush 又会申请 rec_mutex。

普通非递归 mutex 在同一个线程中重复申请时会永久阻塞。我的修复方式是拆分 data_recorder_flush 和 data_recorder_flush_locked，前者负责加锁，后者要求调用者已经持锁。同时数据库写入使用批量事务，只有提交成功后才清空缓冲。

## 12. 为什么 Data Bus 不能在锁内执行回调？

**参考回答：**

回调执行时间不可控，可能访问数据库、UI 或其他模块。在锁内执行会让慢回调阻塞其他发布者，也可能因为回调重入 Data Bus 而死锁。

现在锁内只复制有效回调，随后解锁，再在锁外执行回调。

## 13. Data Bus 回调执行很长会有什么影响？

**参考回答：**

它不会继续占用 Data Bus 内部锁，但仍然会占用执行该回调的线程。如果回调执行数据库、网络或复杂计算，仍然可能拖慢发布线程。

更进一步可以把回调转换为消息投递，由独立消费者线程异步处理。

## 14. 后台线程为什么不能直接更新 LVGL？

**参考回答：**

LVGL 对象树和绘制状态通常要求在同一个 UI 线程访问。后台线程直接操作 LVGL 可能导致对象并发访问、内部状态破坏和随机崩溃。

当前 CAN 接收线程只保存原始帧和解析信号，主线程通过定时器消费这些数据，然后调用 LVGL API 更新界面。

## 15. CAN 数据如何从接收线程传递到 UI 主线程？

**参考回答：**

CAN 接收线程将原始帧、信号名称、单位和物理值写入受 can_ui_mutex 保护的待处理缓冲。

主线程的 timer_1s_cb 读取并复制这些数据，释放锁后再调用 LVGL 页面更新函数。

## 16. 哪些状态使用 atomic，哪些使用 mutex？

**参考回答：**

单个运行标志，例如 MQTT、Modbus、CAN、NTP 和 Watchdog 状态，适合使用 atomic。

多个字段需要保持一致时使用 mutex，例如温湿度和 valid、CAN 原始帧和信号、OTA 状态和进度、HTTP 活跃客户端数量以及记录器缓冲区。

## 17. 服务停止时为什么要先改变状态，再 join？

**参考回答：**

先设置运行状态为 false，让线程知道应该退出；通过超时、关闭事件或条件变量让线程离开阻塞点；再 join 等待线程真正结束。

确认线程不再访问资源后，才能关闭串口、socket、显示 fd 或释放内存。

## 18. 线程阻塞在 read、select 或 socket 接收上，如何保证退出？

**参考回答：**

CAN 的 select、RS485 读取、HTTP 接收和 NTP socket 都设置了超时。停止函数修改运行状态，线程从阻塞调用返回后检查状态并退出。

这样可以避免线程无限期停在阻塞调用中。

## 19. HTTP 服务停止时为什么要等待活跃客户端？

**参考回答：**

HTTP 客户端线程可能仍在访问共享 Web 数据或发送响应。如果主线程直接释放这些资源，客户端可能访问悬空指针或已经关闭的 fd。

当前维护活跃客户端计数，客户端退出时递减；服务停止时通过条件变量等待活跃客户端归零，然后再释放共享资源。

## 20. 如何证明项目不存在数据竞争？

**参考回答：**

当前不能声称已经通过动态工具证明不存在所有数据竞争。

我已经完成主要共享状态的同步、UI 线程边界、服务停止和资源清理路径，并做过源码级检查和 C11 语法检查。但还没有运行 ThreadSanitizer、Helgrind 或长时间并发压力测试。

## 21. 固定大小缓冲区满了以后如何处理？

**参考回答：**

数据记录器在数据库持续失败并且缓冲达到上限时，按照当前策略丢弃新记录；CAN UI 待处理数据是有限缓冲，主线程消费不及时可能覆盖旧数据或丢失中间帧。

如果业务要求不丢数据，需要使用环形队列、持久化队列、背压或明确的告警机制。

## 22. 取消订阅和发布同时发生时，回调还会执行吗？

**参考回答：**

当前发布时会先复制一份回调快照。如果回调已经进入快照，之后再取消订阅，这次发布仍可能执行一次回调。

如果以后要求取消订阅后立即停止所有回调，需要增加引用计数、订阅版本号或回调生命周期管理。

---

# 三、MQTT 和温湿度数据

## 23. RK3506 使用的 Broker、端口和 Topic 是什么？

**参考回答：**

当前配置是 Broker 192.168.5.10，端口 1883，Topic 为 esp32c6/sensor，Keepalive 为 60 秒。默认用户名和密码为空，当前没有配置 TLS。

RK3506 作为 MQTT 客户端主动连接 Broker，接收温湿度 JSON，也可以向该主题发布数据。

## 24. MQTT 认证为什么要在启动连接前设置？

**参考回答：**

异步 MQTT 客户端启动后，第一次连接可能立即发生。如果先启动连接、后设置用户名和密码，首次连接可能没有认证信息。

当前顺序是：

~~~text
mqtt_client_init()
    -> mqtt_client_set_auth()
    -> mqtt_client_start()
~~~

这样可以确保第一次连接使用正确的认证配置。

## 25. MQTT 使用什么 QoS？是否使用 retain？

**参考回答：**

当前 UI 发布温湿度数据时使用 QoS 1，retain 为 false。

QoS 1 是至少一次投递，可能重复，不能等价于业务层 exactly once。当前项目没有应用层命令 ID、ACK 和去重机制。

## 26. MQTT 断线后如何重连？

**参考回答：**

当前 MQTT 使用异步连接和重连状态，重连间隔配置为 5、10、30、60 秒。连接状态和重试次数通过状态接口提供。

重连主要恢复后续消息接收，当前没有充分证据证明断线期间的数据或控制命令会进入持久化队列并在重连后补发。

## 27. 重连间隔是如何设计的？

**参考回答：**

当前使用逐步增加的重连间隔，避免 Broker 不可用时设备高频重连，减少网络和 CPU 消耗。

5、10、30、60 秒是当前配置行为，不代表已经通过复杂网络故障场景验证了最佳参数。后续可以增加最大退避时间、随机抖动和网络恢复检测。

## 28. RK3506 端如何解析温湿度 JSON？

**参考回答：**

当前使用 cJSON 解析 JSON，要求 temperature 和 humidity 是 JSON 数值：

~~~json
{
  "temperature": 25.6,
  "humidity": 48,
  "valid": true
}
~~~

如果字段是字符串，例如 temperature 为 "25.6"，当前代码不会按数值接受，因为它检查的是 cJSON_IsNumber。

valid 字段可选，缺省时当前默认按有效数据处理。

## 29. valid=false 和数据过期有什么区别？

**参考回答：**

valid=false 是数据源主动表示当前数据无效；数据过期是设备在一段时间没有收到新消息后，根据时间判断旧数据不再可信。

当前代码支持 valid 字段，但没有完整的 last_update、TTL 或 stale 机制，不能把 MQTT 断线自动等同于数据过期。

## 30. 当前有没有 last_update、TTL 或 stale 机制？

**参考回答：**

当前没有完整的 last_update、TTL 或 stale 机制。

系统可以识别 MQTT 连接状态，也可以读取消息中的 valid 字段，但长时间没有新消息时不会自动根据时间判断数据过期。断线后可能继续显示最后一次温湿度，只是 MQTT 状态显示未连接。

后续可以记录最后接收时间，使用 now - last_update > TTL 判断 stale，并同步更新 UI、Web 和数据库查询结果。

## 31. MQTT 消息重复到达时，当前如何处理？

**参考回答：**

当前温湿度消息主要按照收到顺序更新最新值并记录数据库，没有基于消息 ID 或时间戳的去重机制。

监测数据重复通常只是多一条记录；控制命令如果重复执行可能产生问题，因此控制场景需要应用层幂等机制。

## 32. MQTT 断线后，最后一次温湿度数据如何处理？

**参考回答：**

当前会保留最后一次温湿度数据，同时通过 MQTT 连接状态显示断线。

系统不会因为断线立即清空温湿度，也不会自动标记 stale。使用方需要结合连接状态判断数据是否仍然新鲜。

## 33. 如何避免旧数据被误认为实时数据？

**参考回答：**

当前项目还没有完整解决这个问题。后续我会增加 last_update、TTL、stale、数据源连接状态和数据质量字段。

Web 和 UI 显示时同时给出数值、更新时间和新鲜度，而不是只返回温湿度数值。

## 34. 项目有没有端到端控制时延和丢包率数据？

**参考回答：**

当前没有正式的端到端时延、丢包率、重连耗时或成功率测试数据。

源码层面可以确认 MQTT 使用 QoS 1、重连间隔为 5/10/30/60 秒、使用异步连接，并提供连接状态和重试计数。我不会编造具体毫秒数或百分比。

## 35. MQTT 能不能保证控制命令只执行一次？

**参考回答：**

不能直接保证。QoS 1 是至少一次投递，网络或客户端恢复过程中可能出现重复消息。

当前没有应用层命令 ID、ACK、幂等键和去重表。如果用于严格控制，我会增加命令序列号、执行状态、结果 ACK、超时重试和幂等处理。

---

# 四、Modbus RTU 和 RS485

## 36. Modbus 使用什么串口、波特率和从站配置？

**参考回答：**

当前默认配置是串口 /dev/ttyS3、波特率 9600、方向控制 GPIO0、轮询周期 1000 ms、从站地址 1、功能码 3、起始地址 0、寄存器数量 2。

## 37. Modbus RTU 请求帧如何构造？

**参考回答：**

当前使用手工实现的 RTU 帧，包含从站地址、功能码、起始地址、寄存器数量和 CRC16。

发送前按照 Modbus RTU 字节顺序组帧，发送后根据响应地址、功能码、字节数和 CRC 检查数据。

## 38. CRC16 如何计算和校验？

**参考回答：**

CRC16 用于验证 RTU 帧在传输过程中是否发生错误。接收响应后重新计算 CRC，并与帧尾 CRC 比较；不一致时丢弃该帧，不更新业务数据。

## 39. 收到响应后需要检查哪些字段？

**参考回答：**

当前会检查从站地址、功能码、是否为异常响应、响应总长度、字节数和 CRC16。

只检查长度是不够的，否则可能把其他设备数据、半包或异常帧误当成有效数据。

## 40. 如何处理 Modbus 异常响应？

**参考回答：**

Modbus 异常响应通常表现为功能码最高位被置位。当前代码会检查功能码和响应长度，识别异常后不把它当成正常寄存器数据。

异常情况应该通过日志和状态接口暴露，而不是直接更新成正常业务值。

## 41. RS485 收发方向如何控制？

**参考回答：**

当前通过 GPIO0 控制 RS485 收发方向。发送前切换到发送状态，发送完成后切换到接收状态，再等待响应。

方向控制、串口读写和关闭路径需要协调，避免多个线程同时使用串口或关闭设备。

## 42. Modbus 线程如何停止？

**参考回答：**

停止时先修改 Modbus 运行状态，读取操作通过超时返回，线程检查状态后退出，主线程再 join 线程并关闭 RS485 fd。

## 43. 如果从站没有响应，线程会不会永久阻塞？

**参考回答：**

当前 RS485 读取使用超时机制，Modbus 线程不会无限等待设备响应。超时后结束本次轮询并继续后续状态处理。

如果需要提高可观测性，可以增加超时次数、连续失败计数和设备离线状态。

## 44. 当前项目是否使用 libmodbus？

**参考回答：**

当前没有使用 libmodbus 适配层，Modbus RTU 是手工构造请求和校验 CRC。

优点是可以直接控制帧格式和 RS485 时序，代价是协议边界、超时、异常响应和多设备管理都需要自己维护。

## 45. Modbus 数据有没有接入 SQLite 和 Web？

**参考回答：**

当前项目有 Modbus 数据采集、Data Bus 发布和设备状态接口，但不能把 device_data 表或数据库函数直接说成完整的 Modbus 历史 Web 查询产品。

Web 当前主要提供当前设备数据和状态，历史查询路由已经去掉。

## 46. 如果总线上增加多个从站，如何扩展？

**参考回答：**

可以将从站配置抽象成设备表，每个设备包含地址、功能码、起始地址、寄存器数量、轮询周期和解析规则。

轮询线程按设备表发送请求，Data Bus、SQLite 和 Web API 都按设备 ID 区分数据。同时要保证总线访问串行化，避免单个设备超时拖住全部设备。

---

# 五、CAN 和 SocketCAN

## 47. CAN 使用什么 Linux 接口？

**参考回答：**

当前使用 Linux SocketCAN RAW，创建 PF_CAN、SOCK_RAW、CAN_RAW socket，绑定 can0，通过 select 等待帧，读取 can_frame 后解析信号并发布到 Data Bus。

默认接口是 can0，比特率是 500000。

## 48. SocketCAN RAW socket 如何创建和绑定？

**参考回答：**

先创建 PF_CAN、SOCK_RAW 类型 socket，获取 can0 的接口索引，构造 sockaddr_can 并调用 bind。绑定成功后使用 read 读取 struct can_frame。

当前通过 select 设置等待超时，便于线程停止。

## 49. 为什么 CAN 使用 select？

**参考回答：**

select 可以同时等待数据和定期检查退出状态。当前等待大约 1 秒，超时后线程检查运行标志。

如果一直阻塞在 read，停止服务时可能无法及时退出，导致 join 卡住。

## 50. CAN 信号如何从原始帧解析成物理值？

**参考回答：**

先根据 CAN ID 匹配信号配置，再按照起始位和信号长度提取原始值，最后执行：

~~~text
physical_value = raw_value * scale + offset
~~~

当前默认示例信号为 CAN ID 0x123、起始位 0、长度 16、比例 0.25、偏移 0、单位 rpm。

## 51. CAN 信号解析需要注意哪些问题？

**参考回答：**

需要确认 CAN ID、起始位、信号长度、字节序、位序、符号位、比例、偏移、DLC 和数值范围。

当前已经实现按信号配置解析、长度检查和物理值换算，但复杂协议的所有端序和缩放场景还需要通过真实报文测试验证。

## 52. 当前默认 CAN ID 和信号配置是什么？

**参考回答：**

当前默认 CAN ID 是 0x123，信号起始位 0，长度 16，scale 为 0.25，offset 为 0，单位为 rpm，信号名称为 Engine RPM。

这是示例配置，不能描述成覆盖完整车辆或工业 CAN 数据字典。

## 53. CAN 高负载时会不会丢帧？

**参考回答：**

不能保证高负载下完全不丢帧。当前设计主要解决线程安全和 UI 并发访问问题，待处理数据使用固定缓冲。

主线程卡顿或消费速度不足时，可能覆盖旧数据或丢失中间帧。若业务要求完整记录，需要环形队列、专用记录线程或持久化队列。

## 54. CAN 接收线程如何把数据交给 UI？

**参考回答：**

CAN 接收线程不直接操作 LVGL，而是将原始帧和解析信号写入受锁保护的待处理缓冲。主线程定时器复制缓冲内容，释放锁后调用 UI 页面函数。

## 55. 当前是否实现了 CAN UI 过滤器？

**参考回答：**

当前没有完整的 CAN UI 过滤器产品链路。底层存在过滤相关能力，但没有足够证据表明 UI 支持过滤器配置，因此项目不宣传 CAN UI Set Filter 功能。

## 56. can_socket.c 中为什么还存在 system？

**参考回答：**

该 system 调用用于配置 CAN 接口，例如设置接口和比特率。当前命令参数来自编译期配置，不是 HTTP 或 MQTT 直接传入的外部字符串，因此当前没有发现直接的外部命令注入链路。

如果以后改为外部配置，就应该使用直接系统调用或严格白名单校验。

## 57. CAN 接收线程如何处理异常帧和 DLC？

**参考回答：**

接收后先检查读取长度是否等于 CAN 帧结构大小，再根据 DLC 限制实际数据复制长度。

无法匹配的 CAN ID、DLC 不合法或数据长度不足的帧，应记录日志或丢弃，不能更新成有效业务信号。

---

# 六、HTTP 和 Web

## 58. HTTP 服务采用什么模型？

**参考回答：**

当前采用监听线程加 detached 客户端线程：

~~~text
监听线程
    -> accept
    -> 创建客户端线程
    -> 读取请求
    -> 路由处理
    -> 发送响应
~~~

最大活跃客户端数量限制为 10。

## 59. HTTP 当前支持哪些接口？

**参考回答：**

当前主要支持 /api/sensor/current、/api/system/info、/api/health、/api/status、/api/device/list、/api/device/modbus、/api/device/can、/api/ota/check、/api/ota/status、/api/ota/start 和静态首页 /。

当前没有宣传 Web 历史数据查询。

## 60. HTTP 请求解析做了哪些保护？

**参考回答：**

当前增加了单请求 4096 字节限制、Content-Length 处理、接收超时、超大请求返回 413、活跃客户端数量限制和发送循环。

它仍然是面向受控设备网络的轻量 HTTP 服务，不是完整的 HTTP/1.1 服务器。

## 61. 如果客户端分片发送请求，当前能否完整处理？

**参考回答：**

当前对有限请求体和 Content-Length 做了多次接收处理，比一次 recv 后直接解析更加可靠。

但它不是完整 HTTP/1.1 实现，不能宣称完整支持所有复杂分片、chunked 编码、长连接和协议扩展场景。

## 62. 为什么需要限制客户端数量？

**参考回答：**

每个客户端线程都会消耗线程栈、文件描述符和调度资源。没有上限时，异常客户端可能耗尽设备资源。

当前限制为 10 个活跃客户端，超过限制时拒绝或关闭新的连接。

## 63. HTTP 短写如何处理？

**参考回答：**

send 一次不一定发送完整响应，因此当前使用循环发送剩余数据，直到全部发送完成或发生错误。

这样可以避免客户端只收到半段 JSON 或半个 HTTP 响应。

## 64. HTTP 客户端线程为什么使用 detached？

**参考回答：**

客户端数量动态变化，使用 detached 线程可以在线程退出时自动释放线程资源，不需要主线程维护每个客户端的 join。

因为 detached 线程不能 join，所以代码额外维护活跃客户端计数，服务停止时等待计数归零。

## 65. 当前 HTTP 有没有认证和 HTTPS？

**参考回答：**

当前没有完整的 HTTP 认证、Token、HTTPS 或 TLS。HTTP 是明文服务，并且监听在 INADDR_ANY。

它适合受信任的设备网络或外部认证网关后面，不能直接当作互联网暴露服务。

## 66. INADDR_ANY 有什么安全风险？

**参考回答：**

INADDR_ANY 会监听本机所有网络接口。如果设备存在多个接口或网络不可信，HTTP API 可能被非授权客户端访问。

生产部署前需要限制网络访问，或者绑定指定管理网卡，并增加认证授权和加密传输。

## 67. 如果客户端一直不发送完整请求，会发生什么？

**参考回答：**

当前设置了接收超时。客户端长时间不发送完整请求时，线程会超时退出并释放连接，同时请求总大小限制为 4096 字节。

## 68. Web API 返回的数据是否一定是最新数据？

**参考回答：**

不一定。Web API 读取的是当前缓存的温湿度和服务状态，但当前没有 last_update、TTL 或 stale。

MQTT 断线后，Web 可能继续返回最后一次温湿度数据。因此后续应返回数值、最后更新时间、valid、stale 和连接状态。

## 69. 为什么 Web 历史接口被移除？

**参考回答：**

数据库层存在历史查询和统计函数，但 Web 路由没有连接完整的真实查询链路。

为了避免把底层残留函数说成产品能力，我移除了未完成的 Web 历史路由，只宣传实时状态、设备状态和 OTA 接口。

## 70. HTTP 服务停止时如何避免客户端访问已释放数据？

**参考回答：**

停止时先设置服务停止状态，停止接收新的客户端，然后等待活跃客户端数量归零。

客户端退出前释放自身资源并递减计数，主线程收到条件变量通知后，才释放 Web 共享数据和服务锁。

## 71. 如何防止 OTA API 被未授权调用？

**参考回答：**

当前项目还没有完整的认证授权机制，因此不能说已经解决。

后续需要增加身份认证、Token 或证书认证，限制 OTA 操作权限，并加入版本校验、数字签名、操作审计和防重复提交。

---

# 七、SQLite、数据记录和可靠性

## 72. SQLite 的表结构是什么？

**参考回答：**

核心表 sensor_data 包含 id、timestamp、temperature、humidity 和 valid。timestamp 有索引，用于历史查询和按时间清理。

数据库中还存在 device_data 表，但表存在不等于完整产品功能已经接通。

## 73. 为什么给 timestamp 建索引？

**参考回答：**

历史查询和数据清理通常按照时间范围筛选。timestamp 索引可以减少全表扫描，提高按时间查询和删除旧数据的效率。

具体性能提升仍需通过实际数据量和目标设备测试确认。

## 74. 为什么采用批量事务写入？

**参考回答：**

每条数据单独提交会增加事务、锁和文件系统同步开销。批量事务可以减少事务次数，提高写入效率。

当前记录器最多积累约 120 条，或者按约 60 秒间隔刷库。事务提交成功后才清理内存记录。

## 75. 数据库写入失败时，内存缓冲如何处理？

**参考回答：**

数据库写入失败时不会立即清空内存缓冲。只有事务 COMMIT 成功后，记录器才清理已经写入的记录。

如果数据库持续失败，数据会保留并等待后续重试；如果缓冲达到上限，当前策略可能丢弃新记录。

## 76. 数据库写入失败会不会丢失数据？

**参考回答：**

在缓冲没有耗尽的情况下，当前代码会保留失败批次，降低一次写入错误造成的数据丢失。

但如果数据库长时间不可用，缓冲最终可能达到上限，因此不能宣称数据库异常时绝对不丢数据。

## 77. 数据记录器的刷盘间隔和批次大小是多少？

**参考回答：**

当前最大批次约 120 条，刷盘间隔约 60 秒，数据保留时间配置为 30 天。

批次越大，单次写入效率通常越高，但异常掉电前未提交的数据也可能更多。

## 78. 数据库 timestamp 是不是实时数据过期依据？

**参考回答：**

不是。SQLite timestamp 主要用于记录保存时间、历史查询和数据清理。

当前实时温湿度状态没有独立的 last_update 和 TTL 判断。要支持 stale，需要在实时状态结构中单独保存最后接收时间。

## 79. 数据库历史查询函数是否已经接入 Web？

**参考回答：**

没有形成完整 Web 产品链路。数据库层存在 database_query_history 和 database_get_stats，但 Web 历史路由已经去掉。

我会说数据库层具备查询基础函数，而不是说 Web 已实现完整历史查询。

## 80. SQLite 连接是否支持多个线程并发访问？

**参考回答：**

当前主要由数据记录器负责写入，数据库连接的通用多线程并发边界还需要进一步明确。

如果以后增加多个并发查询和写入线程，我会增加统一数据库互斥锁、连接池或单线程数据库访问模型。

## 81. 数据保留策略如何实现？

**参考回答：**

清理逻辑根据当前时间减去保留天数计算 cutoff，然后删除早于 cutoff 的 sensor_data 和 device_data 记录。

当前配置为保留 30 天，具体清理周期和磁盘压力需要结合目标板长期运行验证。

## 82. 如果数据库损坏或磁盘空间不足，系统如何处理？

**参考回答：**

当前代码能够通过返回值判断数据库打开、插入或提交失败，记录器会保留失败缓冲并等待重试。

数据库修复、磁盘满后的告警、自动迁移和数据导出还没有完整实现。我会说“有失败检测和缓冲保留，但异常恢复能力仍需完善”。

---

# 八、OTA 和系统可靠性

## 83. OTA 的完整流程是什么？

**参考回答：**

当前流程是：

~~~text
检查版本
    -> 下载到临时文件
    -> 校验长度和 SHA-256
    -> 备份旧应用
    -> fsync 临时文件
    -> 原子 rename
    -> fsync 父目录
    -> fork/exec 启动新应用
    -> 新应用写健康标记
    -> 健康检查失败则回滚
~~~

下载阶段不会直接覆盖正在运行的应用。

## 84. 为什么不能使用 shell 脚本直接替换应用？

**参考回答：**

shell 命令会带来参数转义、命令注入、文件替换不原子、脚本错误处理不透明和异常掉电状态不易控制等问题。

当前改成直接文件操作、SHA-256 校验、临时文件、fsync、原子 rename、fork/exec 和直接调用 reboot。

## 85. 下载文件为什么不能直接覆盖正在运行的程序？

**参考回答：**

直接覆盖时，如果下载过程中断电或网络失败，可能留下半个文件，设备下次启动时无法运行。

当前先下载到临时文件，校验通过后再备份旧文件并执行原子替换。

## 86. OTA 如何进行完整性校验？

**参考回答：**

当前检查下载长度和 SHA-256 摘要，只有校验通过才进入安装步骤。

SHA-256 能发现文件内容变化，但不能证明发布者身份。当前默认没有完整数字签名和公钥管理。

## 87. SHA-256 能不能证明升级包可信？

**参考回答：**

不能完全证明。SHA-256 主要验证内容完整性，如果攻击者能够同时替换升级包和摘要，单纯 SHA-256 不能证明文件来自可信发布者。

发布者认证还需要数字签名、可信公钥、密钥轮换和防回滚策略。

## 88. 临时文件、备份文件和原子 rename 分别解决什么问题？

**参考回答：**

临时文件避免未完成下载直接覆盖正式应用；备份文件用于新版本启动失败时恢复旧版本；原子 rename 尽量保证文件名切换不会暴露半成品；fsync 尽量保证文件内容和目录元数据落盘。

## 89. 为什么还要同步父目录？

**参考回答：**

文件内容同步不等于文件名替换关系已经持久化。rename 会修改父目录元数据，替换后同步父目录可以提高掉电后文件名状态保持一致的概率。

## 90. 新应用如何确认自己启动成功？

**参考回答：**

新应用完成基本初始化后写入健康标记文件，例如 /tmp/ota_ok。更新流程等待健康窗口，如果标记没有出现，就恢复备份并重启。

## 91. OTA 失败后如何避免设备进入不可启动状态？

**参考回答：**

主要依靠四层保护：下载不直接覆盖正式文件，安装前校验长度和 SHA-256，替换前备份旧版本，新版本启动失败时通过健康标记触发回滚。

当前代码具备这条回滚控制流，但真实掉电和文件系统异常场景还需要测试。

## 92. OTA 是否支持 HTTPS 和数字签名？

**参考回答：**

当前默认 OTA 使用 HTTP，代码有可选签名校验回调接口，但没有配置完整签名实现和公钥管理。

因此当前实现的是完整性校验、原子替换和回滚框架，安全传输和发布者身份认证仍需完善。

## 93. OTA 更新过程中为什么使用 fork/exec？

**参考回答：**

更新过程中需要切换正在运行的应用。通过 fork/exec 可以启动新版本，让新版本完成初始化和健康确认。

这属于更新交接流程，不是正常业务采用多进程架构。

## 94. OTA 重复点击会发生什么？

**参考回答：**

OTA 状态和运行标志受锁保护，检查、下载或更新过程中会判断是否已经处于运行状态，避免同时启动多个更新任务。

如果当前处于忙状态，UI 或 Web 入口应该返回忙状态，而不是重复执行更新。

## 95. 如果 OTA 过程中断电，是否绝对不会损坏？

**参考回答：**

不能绝对保证。当前通过临时文件、校验、备份、fsync、原子 rename 和健康标记降低风险。

真实文件系统、突然断电、存储介质损坏和启动环境仍需要在目标板上做故障注入测试。

---

# 九、LVGL、DRM 和资源管理

## 96. LVGL 为什么要求主线程访问？

**参考回答：**

LVGL 的对象树、样式和绘制状态通常不是为多个业务线程同时修改设计的。

当前后台线程只更新业务数据和待处理缓冲，主线程统一调用 LVGL API，避免对象树竞态。

## 97. DRM 显示初始化包含哪些步骤？

**参考回答：**

当前显示模块涉及打开 DRM 设备、获取显示资源、创建或设置 framebuffer、建立 mmap 映射，并将显示缓冲交给显示刷新流程。

退出或初始化失败时，需要按已成功申请的资源逆序释放。

## 98. framebuffer 映射和 DRM fd 如何释放？

**参考回答：**

退出时先解除 framebuffer 的 mmap，再释放 DRM framebuffer 和相关资源对象，最后关闭 DRM fd。

初始化中途失败时，也要根据已完成的步骤执行局部清理。

## 99. evdev 输入设备如何初始化和关闭？

**参考回答：**

目标平台使用 evdev 提供输入。初始化时打开设备并接入 LVGL 输入驱动，退出时停止使用、关闭 fd 并释放相关对象。

当前已经补充主要清理路径，但没有设备拔插和反复初始化的动态测试报告。

## 100. 初始化中途失败时如何清理资源？

**参考回答：**

我采用分阶段初始化和失败回滚思路，每完成一步就记录资源状态，后续步骤失败时只释放已经成功申请的资源。

清理按照依赖关系逆序执行，例如先停止线程，再关闭设备 fd，最后释放上层对象。

## 101. 如何避免线程退出后 fd 仍被使用？

**参考回答：**

所有主要服务都遵循先停止线程、再 join、最后关闭 fd 的顺序，同时通过超时、运行状态和停止标志降低关闭路径中的并发访问风险。

## 102. 当前有没有 Valgrind、ASan 或长期稳定性测试？

**参考回答：**

当前没有完整的 Valgrind、AddressSanitizer、ThreadSanitizer、Helgrind 或数天级长期稳定性测试报告。

已经完成的是源码级问题修复和 C11 语法检查，后续需要在主机模拟环境和目标板上补充动态工具和长期运行测试。

## 103. OTA 重启和正常退出的资源清理有什么区别？

**参考回答：**

正常退出需要完整停止服务、join 线程、关闭设备并释放资源。OTA 更新时重点是保证文件切换、健康标记和回滚状态正确。

即使随后要重启，也不能在新进程启动前留下不一致的文件和线程状态。

---

# 十、日志和故障排查

## 104. 项目如何判断服务是否正常？

**参考回答：**

可以结合 MQTT 连接状态、MQTT 重试次数、Modbus 运行状态、CAN 运行状态、NTP 同步状态、HTTP 状态、OTA 状态、Watchdog 状态、/api/health、/api/status 和 /tmp/my_test.log。

## 105. 日志为什么只保留一个备份文件？

**参考回答：**

当前目标是避免 /tmp 日志无限增长，同时控制存储占用。日志达到约 1 MiB 后轮转，并保留一个 .1 文件。

优点是实现简单、占用可控；缺点是历史追溯能力有限。正式产品可以增加多级轮转、压缩或按日期清理。

## 106. 如果设备没有温湿度数据，怎么排查？

**参考回答：**

我会按链路排查：

~~~text
Broker 连通性
    -> MQTT 认证
    -> Topic 是否正确
    -> JSON 格式是否正确
    -> temperature/humidity 是否为数字
    -> valid 状态
    -> on_sensor_data()
    -> Web 和数据库更新
    -> UI 主线程消费
~~~

同时结合 health、status 接口和日志，区分连接、解析、线程和 UI 问题。

## 107. 如果系统随机崩溃，你会如何定位？

**参考思路：**

先看日志和崩溃时间点，判断发生在线程、UI、网络、串口、CAN 还是 OTA；再检查锁竞争、越界和 fd 关闭竞态。

工具方面使用 GDB 获取 backtrace，AddressSanitizer 检查内存错误，ThreadSanitizer 检查数据竞争。当前没有完整工具测试报告，因此不能说已经覆盖全部代码。

## 108. 如果 MQTT、HTTP、Modbus、CAN 同时异常，如何排查？

**参考回答：**

先确认进程和主循环是否仍在运行，再查看 health、status 和日志。

多个服务同时异常时，优先检查电源、网络、系统时间、线程退出、文件描述符和资源耗尽；只有单个服务异常时，再检查对应协议参数和外设。

## 109. 如果 MQTT 连接正常但没有温湿度数据，可能是什么问题？

**参考回答：**

可能是 Topic 不一致、消息不是合法 JSON、字段名错误、字段类型是字符串、缺少温湿度字段，或者消息没有通过 cJSON 字段校验。

我会先抓取实际 payload，再对照解析条件和日志，不会只看 MQTT 连接状态。

## 110. 如果 Web 返回旧数据，怎么判断是不是程序 bug？

**参考回答：**

先确认 MQTT 是否断线，以及最近是否有新消息。当前项目没有 TTL/stale，所以断线后返回最后一笔数据属于已知行为，不一定是程序崩溃。

如果 MQTT 仍在线且有新消息，再检查 on_sensor_data、Web 共享状态锁和数据更新调用链。

---

# 十一、构建、测试和性能

## 111. CMake 如何区分主机模拟和 ARM 目标？

**参考回答：**

当前统一使用 CMakeLists.txt。HOST_SIMULATION=ON 时构建 my_test_sim，使用 SDL2 和硬件 stub；默认分支构建 ARM 目标 my_test，使用 ARM 交叉编译器和目标依赖。

## 112. 为什么删除 CMakeLists_host.txt？

**参考回答：**

原来两个 CMake 入口存在不同的源文件和模拟实现组织方式，容易造成主机和 ARM 行为不一致，也容易被历史构建目录误导。

现在通过一个 CMakeLists.txt 和 HOST_SIMULATION 选项区分目标。

## 113. 主机模拟是否等价于真实目标板？

**参考回答：**

不等价。主机模拟主要验证代码组织、接口和部分业务逻辑，显示、触摸、串口、CAN、MQTT、数据库和 OTA 可能使用 stub 或主机依赖。

主机模拟通过不能替代 RK3506G 目标板上的驱动和外设联调。

## 114. 主机模拟构建需要哪些依赖？

**参考回答：**

主机模拟需要 SDL2 头文件和库，同时使用项目中的 LVGL 和主机 stub。

当前环境缺少 SDL2 开发依赖，因此 CMake 能到达 SDL2 检查，但没有完成最终主机链接运行。

## 115. ARM 构建需要哪些环境？

**参考回答：**

ARM 构建需要 Buildroot 或其他交叉工具链、目标 sysroot，以及 LVGL、Mosquitto、SQLite3、cJSON、DRM、evdev 等目标依赖。

当前环境没有完整目标板 SDK/sysroot，因此没有执行真实 ARM 链接和目标板启动验证。

## 116. 当前已经做过哪些验证？

**参考回答：**

当前完成了多文件 C11 语法级检查，覆盖入口、UI、Data Bus、Data Recorder、数据库、MQTT stub、Modbus、CAN、OTA Web API 和日志模块。

CMake 主机模拟配置能够到达 SDL2 依赖检查，但因为 SDL2 缺失没有完成最终链接。准确说法是源码级验证完成，目标板全链路验证待补。

## 117. 项目有没有单元测试、集成测试和压力测试？

**参考回答：**

当前项目没有接入 CMake 的专用单元测试、集成测试和压力测试套件。lvgl_src 中的第三方测试内容不等于本项目业务测试。

后续可以为 CRC、CAN 信号解析、HTTP 请求解析、Data Bus 并发和 OTA 状态机增加测试。

## 118. 项目当前有没有 CPU、内存、吞吐量和 P99 延迟数据？

**参考回答：**

当前没有正式 benchmark、压力测试或长期运行报告，所以我不会给出 CPU、内存、吞吐量、P99 延迟、CAN 最大帧率或 OTA 成功率。

当前只能说明源码设计约束，例如 HTTP 请求上限 4096 字节、客户端上限 10、记录批次 120、OTA 最大下载 16 MiB。

## 119. 如果要做性能测试，你会测哪些指标？

**参考思路：**

我会测试 MQTT 端到端延迟和重连时间、CAN 接收帧率和丢帧率、Modbus 轮询周期和超时比例、HTTP 并发和响应延迟、SQLite 批量写入耗时、OTA 下载和安装耗时，以及 CPU、内存、线程数、fd 数量和长时间资源变化。

所有指标都要在固定硬件、网络和数据规模下采集。

## 120. 如何使用 GDB、strace、perf 或 sanitizer？

**参考思路：**

GDB 用于定位崩溃线程和调用栈；strace 观察 socket、文件和线程相关系统调用；perf 分析 CPU 热点和调度；AddressSanitizer 检查内存错误；ThreadSanitizer 检查数据竞争；Helgrind 辅助分析锁。

需要先让主机模拟环境可运行，再把关键场景迁移到目标板。

## 121. 主机模拟构建通过，能否说明 ARM 版本没问题？

**参考回答：**

不能。主机模拟和 ARM 目标在编译器、ABI、系统头文件、驱动库、设备节点和运行时行为上都可能不同。

主机构建只能发现一部分语法和接口问题，ARM 版本仍必须使用目标工具链完成编译、链接和设备运行验证。

---

# 十二、设计权衡和改进

## 122. Data Bus 和直接函数调用相比有什么优缺点？

**参考回答：**

Data Bus 的优点是模块解耦，MQTT、Modbus、CAN、记录器和 UI 不需要彼此强依赖。

缺点是数据流不如直接调用直观，回调生命周期更复杂，还要处理锁、订阅槽位和回调重入。

## 123. 为什么不直接使用消息队列传递所有数据？

**参考回答：**

当前数据类型较少、模块数量有限，Data Bus 和受锁保护的状态缓冲实现成本更低。

如果后续数据量增加，尤其是 CAN 高频帧或控制命令，我会考虑环形队列或 POSIX 消息队列，并增加 ACK 和背压。

## 124. 当前项目最需要增加的机制是什么？

**参考回答：**

我认为最优先的是三项：温湿度 last_update、TTL 和 stale 状态；OTA 数字签名和可信公钥管理；HTTP 认证、TLS 和权限控制。

这三项分别对应数据可信性、升级安全和远程管理安全。

## 125. 如果要求实现严格的控制闭环，你会怎么改？

**参考回答：**

我会增加命令唯一 ID、发送时间和过期时间、执行状态、设备 ACK、执行结果、超时重试、幂等去重、本地持久化队列、重连补偿和审计日志。

MQTT QoS 1 不能代替业务层控制协议。

## 126. 如果支持多个传感器设备，需要改什么？

**参考回答：**

需要把当前单一全局温湿度状态扩展成设备维度的数据结构：

~~~text
device_id
temperature
humidity
valid
last_update
stale
~~~

Data Bus、SQLite、Web API 和 UI 都需要按照设备 ID 区分数据。

## 127. 如果把 HTTP 拆成独立进程，如何通信？

**参考思路：**

可以使用 Unix Domain Socket、共享内存加同步机制、消息队列或本地 TCP。

如果主要传递状态和控制命令，我会优先考虑 Unix Domain Socket 或消息队列，并定义请求 ID、响应 ID、超时和权限控制。

当前项目没有实现这套多进程架构，只能作为后续设计方案。

## 128. 如果要支持实时数据质量管理，需要增加什么？

**参考回答：**

我会将数据状态拆成 value、valid、last_update、stale、source_connected、quality 和 error_code。

这样可以区分传感器主动报错、MQTT 断线、数据过期、格式错误和数值越界。

## 129. 如果数据库和 MQTT 同时异常，数据如何处理？

**参考回答：**

当前 MQTT 接收到的数据会进入记录器，数据库异常时由记录器保留有限缓冲并重试；如果缓冲耗尽，当前策略可能丢弃新数据。

当前没有无限持久化队列或断线补传证据。高可靠场景需要增加本地持久化队列和明确的容量、丢弃和告警策略。

## 130. 如果 UI 主线程卡住，系统应该怎么保护？

**参考回答：**

当前后台线程不会直接依赖 LVGL 才能继续工作，但 UI 待处理缓冲有容量边界。

后续可以增加 UI 卡顿监测、主循环心跳、Watchdog 复位策略和数据队列背压，避免 UI 长时间阻塞后导致数据大量丢失。

---

# 十三、项目缺点和风险

## 131. 当前项目最大的风险是什么？

**参考回答：**

主要风险包括 HTTP 默认明文且没有认证、OTA 默认使用 HTTP 且缺少数字签名、没有正式目标板压力和长稳测试、实时温湿度没有 TTL/stale、固定缓冲高负载下可能丢数据，以及 SQLite 和日志的磁盘异常场景还需要验证。

## 132. 如果面试官问“项目有没有缺点”，怎么回答？

**参考回答：**

我不会回避缺点。当前项目的优点是模块链路完整、线程边界清晰、主要并发问题已经修复；不足是安全和动态验证还不够完善。

HTTP 是受控网络中的轻量接口，OTA 有完整性校验和回滚框架但默认没有数字签名，实时数据有连接状态和 valid 但没有 TTL/stale，目标板压力测试和性能数据也需要补充。

## 133. 这个项目最容易被面试官质疑的地方是什么？

**参考回答：**

最容易被追问的是 Web 历史查询函数存在但没有 Web 调用链、MQTT 断线后没有 TTL/stale、HTTP 没有认证和 TLS、OTA 默认没有数字签名、目标板缺少动态联调证据，以及没有性能基准和压力测试数据。

我会主动承认这些边界，并说明当前已经完成的源码修复和下一步验证计划。

## 134. 如果让你继续完善，你会优先做什么？

**参考回答：**

我会优先增加 HTTP 认证和 TLS、OTA 数字签名和可信公钥管理、目标板全链路联调、last_update/TTL/stale、sanitizer 和并发测试、OTA 故障注入、长期稳定性测试以及固定缓冲溢出策略。

---

# 十四、面试统一回答模板

## 135. 面试官问“你怎么证明这个功能有效”，怎么回答？

**回答模板：**

~~~text
第一层：我具体改了什么代码和控制流
第二层：源码中哪个模块、函数或状态能证明
第三层：已经做了哪些静态或构建级验证
第四层：真实设备、压力、故障注入或长稳测试是否有证据
第五层：没有证据的部分，明确列为下一步验证项
~~~

## 136. OTA 问题的标准回答

**参考回答：**

我完成了临时文件下载、SHA-256 校验、备份、fsync、原子 rename、健康标记和回滚控制流，源码证据在 ota_manager.c。

当前做过源码级检查，但没有在真实 RK3506G 上完成掉电和启动失败注入，因此我会说“回滚机制已实现，目标板异常场景测试待补”，不会说已经验证所有故障场景。

## 137. 并发问题的标准回答

**参考回答：**

我先通过源码检查确定共享数据和锁的关系，再缩小临界区。Data Bus 采用锁内快照、锁外回调；数据记录器拆分锁内和公开 flush；LVGL 统一由主线程更新。

当前已经完成源码级修复和语法检查，但还没有用 ThreadSanitizer 或长期压力测试覆盖所有并发路径。

## 138. 性能问题的标准回答

**参考回答：**

当前项目有 HTTP 请求上限、客户端上限、数据库批次和 OTA 下载上限，但没有正式性能报告。

因此我不会直接给出 QPS、P99 延迟、CPU、内存或丢包率，后续需要在固定硬件、数据规模和网络条件下建立基线。

## 139. 未实现功能的标准回答

**参考回答：**

数据库层有历史查询函数，但 Web 没有完整调用链，所以我没有把它写成 Web 历史查询功能。

底层存在过滤或测试接口，也不等于 UI 已实现产品能力。只有具备完整调用链和可验证行为的功能，才会写入项目亮点。

---

# 十五、不能夸大的内容

## 140. 能不能说已经发生过具体线上事故？

**准确表达：**

我可以说根据源码审计确认并修复了死锁、锁内回调、后台线程访问 LVGL、认证顺序、OTA 文件切换和资源清理等风险。

但没有线上日志、工单、监控记录或故障复现报告，因此不能说线上发生过多少次事故或影响过多少台设备。

## 141. 能不能把源码验证说成真实目标板联调？

**准确表达：**

我完成了源码集成、构建入口整理和多文件 C11 语法检查。主机模拟受 SDL2 依赖缺失影响，ARM 目标构建需要外部工具链和 sysroot。

因此不能宣称已经完成 RK3506G 真实启动、显示、触摸、MQTT、RS485、CAN 和 OTA 全链路联调。

## 142. 能不能给出 HTTP、CAN、Modbus、OTA 压力结果？

**准确表达：**

代码中的请求上限、客户端上限、有限缓冲、批量数据库写入和 OTA 下载上限属于设计约束，不是压力测试结果。

我不会给出没有报告支撑的 QPS、并发数、CAN 帧率、Modbus 稳定时间、OTA 成功率或回滚成功率。

## 143. 能不能把数据库函数说成 Web 历史查询产品？

**准确表达：**

数据库层具备历史查询和统计函数，但 Web 路由没有接入真实查询链路，因此当前 Web 只宣传实时状态、设备状态和 OTA 接口。

## 144. 能不能把明文 HTTP 说成 HTTPS？

**准确表达：**

当前 HTTP 是明文服务，没有认证和 TLS；MQTT 默认也没有 TLS 证据。

它适合受控设备网络。如果部署在不可信网络，需要补充 TLS、认证授权、证书管理、请求校验和审计。

## 145. 能不能把 SHA-256 说成数字签名安全？

**准确表达：**

SHA-256 可以检查文件完整性，但默认没有配置签名校验和公钥管理，不能单独证明升级包来自可信发布者。

## 146. 能不能声称所有数据竞争和资源泄漏都已动态证明不存在？

**准确表达：**

我已经完成主要共享状态同步、UI 线程边界、线程停止/join 和资源清理路径，但没有用 ThreadSanitizer、Helgrind、Valgrind 或长期稳定性测试覆盖所有执行路径。

因此只能说已修复已定位的源码问题，并加强了主要生命周期路径。

---

# 十六、最终复习重点

面试前优先熟悉：

1. 项目整体架构；
2. Data Recorder 死锁；
3. Data Bus 锁外回调；
4. LVGL 主线程访问；
5. MQTT Broker、Topic 和认证顺序；
6. 温湿度 JSON 解析；
7. last_update、TTL、stale 当前未实现；
8. Modbus CRC 和响应校验；
9. SocketCAN 和 CAN 信号解析；
10. HTTP 请求限制和安全边界；
11. SQLite 批量事务和失败保留缓冲；
12. OTA 原子替换和健康回滚；
13. SHA-256 与数字签名的区别；
14. CMake 主机/ARM 分支；
15. 当前测试和性能证据边界；
16. 项目缺点和下一步计划。

## 最终项目主线

~~~text
多协议接入
    -> 单进程多线程协作
    -> Data Bus 解耦
    -> 后台数据与 LVGL 主线程隔离
    -> SQLite 批量持久化
    -> OTA 原子更新和失败回滚
    -> 以源码证据区分已实现能力和待验证边界
~~~
