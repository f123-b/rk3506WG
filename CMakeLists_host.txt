# Deprecated standalone host build file.
#
# The host simulation is maintained by the root CMakeLists.txt so that ARM and
# x86 builds share one source list and one set of public APIs.
#
# Canonical host build:
#   cmake -S . -B build_host -DHOST_SIMULATION=ON
#   cmake --build build_host --parallel
#
# CI uses the same commands in .github/workflows/host-ci.yml.
#
# This file is intentionally kept only as a migration note for older scripts
# that referenced CMakeLists_host.txt. Do not add a second source/stub list here.
